spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/zhongzunfa/spring-cloud-config.git
          search-paths: V1-DEV

  application:
      name: config-server

server:
    port: 9092 #启动端口